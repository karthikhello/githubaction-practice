name: 01- Hello world

on: workflow_dispatch

jobs:
  build:
    steps:
      - name: print a greetings
        run: echo "hellow, github action selfhosted runner"
        
      - name: print multiple lines
        run: |
          echo "computher name : $hostname"
          echo "Today's runner": $runner_os"
